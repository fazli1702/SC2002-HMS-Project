package view;

/**
 * The IDisplayMenu interface defines a method that any implementing class 
 * must override to display a menu and handle user interactions.
 */
public interface IDisplayMenu {
	/**
     * Displays a menu and handles user interactions. 
     * The specific implementation will vary depending on the class that implements the interface.
     */
    public void displayMenu();
}
